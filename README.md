# Motor SIG (Gobernación de Cochabamba)

> **Este archivo es la fuente única de verdad del proyecto** — estado actual, arquitectura, decisiones y roadmap. Antes vivía como `ESTADO_PROYECTO.md`; se renombró a `README.md` el 2026-08-31 para que sea un solo documento de seguimiento, no dos que puedan desincronizarse. Actualizar acá, no crear un documento de estado aparte.

Última actualización: 2026-08-31. Resumen de contexto para retomar el trabajo en una próxima sesión, aquí o con Claude.

## Qué es esto

Evolución de `Version previa/` (antes "Resumen ejecutivo.html", 100% cliente, sin backend). Motor SIG es una web app de Google Apps Script: mapa Leaflet + Leaflet.Draw sobre Bootstrap, con Google Sheets como base de datos y Drive para GeoJSON de capas base. El enfoque nuevo agrega dos frentes que la versión anterior no tenía: **CRUD** de geometrías dibujadas en el mapa, y lectura **dinámica** de las hojas `UD_*` (unidades de la Gobernación) igual que `Version previa` leía el JSON del macro de Excel — sin hardcodear columnas.

Ver también `Version previa/ESTADO_PROYECTO.md` (arquitectura del prototipo cliente-only, decisiones tomadas ahí, sigue vigente como referencia de patrón, no como código a mantener).

## Arquitectura actual

```
000_Codigo.gs           doGet() — entry point
001_utils.gs            include() para plantillas HTML
002_Constantes.gs        SIG_DB (spreadsheet ID), SIG_SHEETS (nombres de hoja)
099_Setup.gs             scripts de configuración manual (correr una vez desde
                         el editor de Apps Script, no se llaman desde el frontend)
100_sigDrive.gs          lee GeoJSON desde Drive por fileId
101_sigCache.gs          SIG_Cache_get/put/remove (CacheService, TTL 6h) +
                         SIG_Drive_getGeoJson ahora sí cachea (antes era pass-through)
110_sigConfig.gs          SIG_getSpreadsheet, SIG_getRows (genérico), SIG_Config_getLayerConfig
115_sigUD.gs              SIG_UD_getSchema/getRows/get (hoja) — motor dinámico
                         genérico para cualquier UD_*, lee CONFIGURACION_COLUMNAS,
                         cacheado con SIG_Cache_*
120_sig_geometry.gs       SIG_Geometrias_list (lectura de SIG_GEOMETRIAS)
130_sig_layers.gs         SIG_Capas_list (lectura de SIG_CAPAS) — agregado hoy
140_Sig_Styles.gs         SIG_Config_getStyles (lectura de SIG_ESTILOS)
150_Sig_Symbols.gs        vacío (stub)
160_SIG_Ribbon.gs         vacío (stub)

Index.html + script_*.html + view_*.html + component_*.html + style_*.html
  Frontend en namespace window.SIG, un módulo IIFE por archivo:
  SIG.Map, SIG.Draw, SIG.Selection, SIG.Modal, SIG.Components, SIG.Form,
  SIG.Layers, SIG.LayerPanel, SIG.Config, SIG.Features, SIG.Styles,
  SIG.Symbols, SIG.Loader, SIG.Ribbon, SIG.Geometry, SIG.Views (toggle
  entre vistas principales), SIG.Table (DataTables), SIG.Summary
  (filtros + tarjetas)

googlesheet drive/SDMAyRH Centralizador.xlsx
  Copia LOCAL de la hoja real (ID en 002_Constantes.gs). Usarla para revisar
  estructura sin tocar la hoja viva. No se sincroniza sola — si cambia la hoja
  real, pedir una copia nueva antes de asumir que esto sigue vigente.

Version previa/
  Prototipo cliente-only anterior. Referencia de patrón para el motor dinámico
  (esquema.js, filtros.js, tabla.js, graficos.js, pdf-designer.js), no se toca.
```

## Modelo de datos real (hoja de cálculo)

```
Catálogos:    USUARIOS, SESIONES, CATEGORIAS, REGIONES, PROVINCIAS, MUNICIPIOS
Unidades:     UD_RIEGOS (375 filas, 32 cols — la única "madura" con datos reales)
              UD_RESIDUOS_SOLIDOS (54 filas), UD_FORESTACIONES (1000 filas, dispersa)
              UD_CUENCAS, UD_GESTION_RIESGOS, UD_FISCALIZACION, UD_BIODIVERSIDAD
              (vacías, sin encabezados todavía)
SIG:          SIG_CAPAS, SIG_GEOMETRIAS, SIG_ESTILOS, SIG_SIMBOLOS, SIG_TIPO
Apoyo:        DOCUMENTOS (ID/URL/ELIMINADO, adjuntos por DOCUMENTOS_ID)
              CONFIGURACION (CODIGO/CATEGORIA/VALOR/DESCRIPCION)
```

Convención consistente en casi todas las hojas: borrado lógico vía columna `ELIMINADO`, no borrado físico. Mantener este patrón en cualquier función de escritura nueva.

### `CONFIGURACION` hoy es solo un diccionario de códigos

Filas actuales: `PK/FK` (tipo de llave), `NUM/DEC` (tipo de dato), `SUM/AVG` (estadística), `BAR/PIE` (gráfico), `SEL/HID` (UI). **Falta la tabla que mapea cada columna de cada `UD_*` a estos códigos** — el equivalente real a `Campos[]` de `Version previa/js/esquema.js` (Tipo/Rol/Agregacion/Orden/Visibilidad). Sin esa tabla no hay motor dinámico. Ver "Camino sugerido" más abajo, paso 2.

### Inconsistencias detectadas entre hojas `UD_*` (a resolver en el diseño de la config dinámica, no antes)

- `UD_RIEGOS.REGIONES_ID` / `PROVINCIAS_ID` / `MINICIPIOS_ID` (typo, falta la U) contienen **texto** ("CONO SUR", "TIRAQUE"), no el ID numérico de `REGIONES`/`PROVINCIAS`/`MUNICIPIOS`, a pesar del sufijo `_ID`.
- `UD_RESIDUOS_SOLIDOS` usa columnas de texto plano: `REGIONES`, `PROVINCIAS`, `MUNICIPIOS`.
- `UD_FORESTACIONES` usa singular (`REGION`/`PROVINCIA`/`MUNICIPIO`) y ahí sí parecen IDs numéricos reales.
- Conclusión: un motor genérico no puede asumir una sola convención de FK geográfica; la tabla de configuración por columna debe decir, por hoja, cómo resolver cada una (¿es texto a comparar contra `nombre`, o ID a comparar contra `ID`?).

## Bugs concretos ya corregidos en esta sesión

1. **`120_sig_geometry.gs:25`** — `tipo:item.TIPO` → `tipo:item.TIPO_ID`. El header real de `SIG_GEOMETRIAS` es `TIPO_ID`; antes toda geometría llegaba con `tipo: undefined`.
2. **`130_sig_layers.gs`** — se agregó `SIG_Capas_list()` (antes era un stub vacío `myFunction(){}`), leyendo la hoja `SIG_CAPAS` real vía el `SIG_getRows` genérico. Se agregó `CAPAS: "SIG_CAPAS"` a `SIG_SHEETS` en `002_Constantes.gs`.

**Pendiente de verificar**: estos cambios se basan en la copia local del Excel, no en la hoja viva de Google Sheets — confirmar ahí que los nombres de columna coinciden exactamente antes de dar esto por probado.

**Todavía no conectado**: `SIG_Capas_list()` no tiene consumidor en el frontend todavía — el dropdown "Capa" en `view_geometry.html` sigue con una sola opción hardcodeada. Se deja así a propósito, para conectarlo cuando se diseñe la UI dinámica (no antes).

## Gaps grandes que faltan (del review de arquitectura, sin tocar aún)

- **CRUD incompleto**: dibujar una geometría arma el objeto (`SIG.Geometry.build`) pero solo hace `console.log`, nunca `google.script.run` — no existe ninguna función de escritura en el backend (`create/update/delete`). Falta también estrategia de generación de ID y `LockService` para escrituras concurrentes.
- Validación ausente cliente y servidor (`Number()` silencioso, `JSON.parse` sin try/catch en `SIG_Geometrias_list`).
- Manejo de errores inconsistente en frontend (`withFailureHandler` solo en `SIG.Loader`).
- Llamada duplicada en `script_main.html` (`SIG_Config_getLayerConfig("MUNICIPIOS")` se pide dos veces al iniciar, una directa y otra vía `loadConfig`).
- Modelo de permisos sin definir (`XFrameOptionsMode.ALLOWALL`, sin verificación de rol en las funciones del backend) — relevante porque `USUARIOS`/`SESIONES` ya existen en la hoja pero no se leen desde ningún `.gs` todavía.
- Archivos cáscara sin contenido ni uso: `150_Sig_Symbols.gs`, `160_SIG_Ribbon.gs` (stubs); `component_layer_panel.html`, `component_popup.html`, `component_style_editor.html`, `script_api_sig.html`, `script_utils.html` (plantillas vacías, ni siquiera incluidas en `Index.html`).

## Camino sugerido (orden acordado, ir marcando al avanzar)

1. ✅ Bugs concretos: `TIPO_ID` + lector de `SIG_CAPAS`.
2. ✅ Diseñar y crear hoja `CONFIGURACION_COLUMNAS` — una fila por columna de cada `UD_*`, reutilizando los códigos de `CONFIGURACION`. Columnas:
   `HOJA | COLUMNA | ETIQUETA | TIPO (NUM/DEC/TEXTO/FECHA) | ROL (PK/FK/dato) | REFERENCIA (a qué catálogo apunta si es FK) | ESTADISTICA (SUM/AVG/ninguna) | GRAFICO (BAR/PIE/ninguno) | UI (SEL/HID/texto) | ORDEN | VISIBLE_TABLA | VISIBLE_MAPA | VISIBLE_REPORTE`
   **Hecho y confirmado**: `099_Setup.gs` → función `SIG_Setup_ConfiguracionColumnas()`, crea/reinicia la hoja y carga las 32 columnas de `UD_RIEGOS` como piloto (única hoja con datos reales completos). Ya se ejecutó una vez en el Apps Script real y funcionó. Es idempotente, se puede volver a correr si se ajustan los valores.
   Decisiones de diseño a revisar/ajustar en la hoja resultante (juicio propio, no verdad absoluta): `REGIONES_ID`/`PROVINCIAS_ID`/`MINICIPIOS_ID` marcadas `TIPO=TEXTO` (por la inconsistencia FK-vs-texto ya documentada arriba); columnas vacías en los datos actuales (`CATEGORIA PROGRAMATICA`, `TRANSFERENCIA`, `FASES`) quedaron ocultas (`VISIBLE_*=false`) hasta que tengan datos; `LONGITUD`/`LATITUD`/`SIG_GEOMETRIAS_ID`/`DOCUMENTOS_ID` ocultas por ser de uso interno (mapa/documentos), no de visualización directa.
   **Ampliado (2026-08-20) a pedido del usuario**: se agregaron las 15 columnas reales de `UD_RESIDUOS_SOLIDOS` a `SIG_Setup_ConfiguracionColumnas()`, mismo archivo `099_Setup.gs` (headers reales sacados de la copia local `googlesheet drive/SDMAyRH Centralizador.xlsx`, no inventados). A diferencia de `UD_RIEGOS`, acá `REGIONES`/`PROVINCIAS`/`MUNICIPIOS` no llevan sufijo `_ID` — son los nombres reales de columna. `URBANO_TONALEDAS` no es un typo nuestro: así está escrito el encabezado real en la hoja, hay que dejarlo tal cual para que `SIG_getRows` lo encuentre. `BOTADERO` quedó como el campo "identidad" del registro (equivalente a `NOMBRE_DEL_PROYECTO` en Riegos); `RIESGO` visible en mapa además de tabla, por ser el dato de seguridad más relevante; `SITUACION_ACTUAL`/`PROYECTO` (texto largo, casi siempre vacío en los datos actuales) ocultos de tabla pero visibles en reporte, igual que `OBSERVACIONES` en Riegos.
   **Para aplicar**: volver a correr `SIG_Setup_ConfiguracionColumnas()` en el editor de Apps Script real (con `099_Setup.gs` ya actualizado ahí). **Ojo**: esa función hace `sh.clear()` y reescribe toda la hoja `CONFIGURACION_COLUMNAS` — si se hizo algún ajuste manual directo en esa hoja desde la última vez que se corrió, se pierde.
   **Corrección (2026-08-31, encontrado por el Homúnculo Soberbia en la revisión de cierre del CRUD)**: la afirmación de arriba ("no hace falta limpiar cache aparte") era cierta **solo** para una unidad que pasa de esquema vacío a esquema con filas (`SIG_UD_getSchema` no cachea un esquema vacío, es cierto) — pero **no** para una unidad que YA tenía esquema cacheado y se le ajusta algo (agregar una columna, cambiar un `VISIBLE_*`). Ahí sí quedaba sirviendo el esquema viejo hasta 6h, sin ningún aviso, y encima `SIG_UD_saveRow` valida contra ese esquema cacheado — una columna recién configurada se descartaba en el guardado. **Corregido**: `SIG_Setup_ConfiguracionColumnas()` ahora invalida `ud_schema_*` de las hojas que toca, al final de la misma función.
   **Ampliado de nuevo (2026-08-31) — reescritura completa de `UD_FORESTACIONES`**: el usuario agregó `Forestacion/AREAS FORESTADAS.xlsx` (Excel crudo, 335 filas, coordenadas UTM zona 20S) y `Forestacion/Areas Forestadas GENERADO.kml` (94 placemarks — 81 puntos + 13 polígonos — ya normalizados, nombre `"MUNICIPIO - COMUNIDAD - PROPOSITO"`, con el Excel como punto de verdad para comparar). Antes de tocar nada se validó que el KML es una traducción fiel del Excel: se convirtieron las coordenadas UTM del Excel a lat/lon (EPSG:32720) y coincidieron **exactas** (8 decimales) contra las del KML; los ~227 puntos del Excel sin match directo contra los 81 puntos sueltos del KML corresponden, casi 1 a 1 (213 vértices + cierres de anillo), a los vértices de los 13 polígonos — nada se perdió ni se fabricó al generar el KML.
   `UD_FORESTACIONES` se reescribió entera (antes tenía ~1000 filas de rango pero solo 6 con datos reales, confirmado por el usuario — no había nada real que preservar) con 13 columnas nuevas: `ID | COMUNIDAD (identidad) | REGION | PROVINCIA | MUNICIPIO | PROPOSITO (6 categorías normalizadas del KML, PIE) | CAMPAÑA | AREA_PLANTADA_HA (SUM) | PLANTINES (SUM) | TIPO_PLANTA | PROPOSITO_ORIGINAL (texto crudo, trazabilidad, oculto de tabla) | SIG_GEOMETRIAS_ID | DOCUMENTOS_ID` — de paso corrige el bug ya documentado de `SIG_GEOMETRIA_ID` (singular) al escribirla bien desde el inicio. `REGION`/`PROVINCIA` se derivaron cruzando el `MUNICIPIO` de cada placemark contra el catálogo real (`MUNICIPIOS`→`Provincia_id`→`REGIONES`) — **15 de las 94 filas quedaron sin resolver** (`INDEPENDENCIA` no existe en `MUNICIPIOS`; `SANTIVAÑEZ` y `Villa Guarlberto Villarroel` existen pero con `Region_id`/`Provincia_id` en 0, sin asignar en el catálogo mismo) — no se inventó el dato, el usuario las va a completar a mano.
   `CAPA_ID=2` ("Áreas Forestadas y Reforestación") y `ESTILO_ID=2` ("Límites / Cobertura Vegetal") ya existían en `SIG_CAPAS`/`SIG_ESTILOS` reales y encajan — no hizo falta crear nada ahí.
   Se generaron `Forestacion/import_SIG_GEOMETRIAS.csv` (94 filas, IDs 175-268, `GEOJSON` con el `Feature` completo — puntos y polígonos con anillo cerrado) e `Forestacion/import_UD_FORESTACIONES.csv` (94 filas, IDs 1-94, vinculadas por `SIG_GEOMETRIAS_ID`) — pendientes de pegar a mano en las hojas reales.
   **Para aplicar**: copiar `099_Setup.gs` (con las 13 filas nuevas de `UD_FORESTACIONES`) y volver a correr `SIG_Setup_ConfiguracionColumnas()`. Reemplazar el contenido de `UD_FORESTACIONES` (borrar lo que hay, pegar el encabezado + `import_UD_FORESTACIONES.csv`) y agregar `import_SIG_GEOMETRIAS.csv` al final de `SIG_GEOMETRIAS` (sin borrar lo existente, son filas nuevas con ID que sigue después del máximo real de 174).
   **Bug encontrado al probar**: `DataTables warning: Requested unknown parameter 'COMUNIDAD'` — el esquema (`CONFIGURACION_COLUMNAS`, correcto) declaraba la columna, pero las filas mostradas no la tenían. Causa: **editar una hoja UD_ a mano en Sheets no invalida el caché de filas** (`ud_rows_<hoja>`, hasta 6h) — solo se invalida cuando el guardado pasa por `SIG_UD_saveRow` (la app). Después de pegar el CSV a mano, la app seguía sirviendo la foto vieja (las ~6 filas que había antes, con el encabezado viejo). **Corregido con una utilidad nueva**, no con un cambio de comportamiento (el caché sigue siendo necesario): `SIG_Cache_resetTodasLasUnidades()` en `099_Setup.gs`, limpia `ud_schema_*`/`ud_rows_*` de todas las unidades a la vez — correr esto después de cualquier edición manual en Sheets (no solo esta vez).
3. ✅ Backend genérico: `SIG_UD_getSchema(hoja)` + `SIG_UD_getRows(hoja)` + `SIG_UD_get(hoja)` en `115_sigUD.gs`, reutilizan `SIG_getRows` ya existente. Cache real en `101_sigCache.gs` (`SIG_Cache_get/put/remove`, `CacheService`, TTL 6h, try/catch por el límite de 100KB por clave; no cachea resultados vacíos para no dejar errores de config "pegados" hasta que expire el TTL) y ya conectado también a `SIG_Drive_getGeoJson`. **Confirmado funcionando en el Apps Script real** con `SIG_Test_UD_RIEGOS` (`099_Setup.gs`): 32 columnas de esquema, 114 filas de `UD_RIEGOS`.
   Nota operativa que costó descubrir: el botón "Ejecutar" del editor de Apps Script llama a la función seleccionada **sin argumentos** — para probar funciones que reciben parámetros (`SIG_UD_get(hoja)`, etc.) hace falta una función envoltorio sin argumentos que la llame por dentro (patrón ya usado en `SIG_Test_UD_RIEGOS`, `SIG_Test_ConfiguracionColumnas`, `SIG_Cache_resetUD_RIEGOS` en `099_Setup.gs`) — reutilizar ese patrón para cualquier prueba manual futura.
4. ✅ Vista Tabla dinámica (portar patrón de `Version previa/js/tabla.js`). Decisión tomada con el usuario: usar **DataTables + jQuery** (como la versión anterior) en vez de una tabla vanilla — se suman como dependencias CDN nuevas en `Index.html`. **Confirmado funcionando en el Apps Script real**, incluidos los 3 ajustes de ribbon pedidos después de la primera prueba.
   Detalle:
   - `Index.html` — se agregó jQuery 3.7.1 + DataTables 1.13.6 (core + estilo Bootstrap 5) por CDN, y los `include()` de `style_table`, `view_table`, `script_table`.
   - `style_table.html` / `view_table.html` (nuevos) — panel `#sig-table-view`, mismo alto que `#map` (`calc(100vh - 155px)`), oculto por defecto, con botón "Volver al mapa".
   - `script_table.html` (nuevo) — módulo `SIG.Table`: `show(hoja)` pide `SIG_UD_get(hoja)` al backend, arma columnas/encabezados de la tabla solo con los campos donde `visibleTabla=true` del esquema (nada hardcodeado), inicializa DataTables; `hide()` vuelve al mapa. Alterna la visibilidad con `#map` a mano (todavía no existe un sistema general de "vistas", ver nota abajo).
   - `script_ribbon_config.html` — nuevo grupo "Datos" con botón "Riego" que llama `SIG.Table.show("UD_RIEGOS")` (hardcodeado a esa unidad a propósito, es la única con `CONFIGURACION_COLUMNAS` cargada hoy).
   **Deuda reconocida**: alternar `#map`/`#sig-table-view` a mano en `SIG.Table.show/hide` funciona para dos vistas, pero no escala si se agregan más (Reportes, otras unidades) — cuando lleguemos ahí conviene un `SIG.Views` genérico en vez de repetir el patrón. Relacionado: cambiar de pestaña en el ribbon (Mapa ↔ Datos) **no** cambia la vista principal — si estás viendo la tabla y click en la pestaña "Mapa", la tabla sigue visible hasta apretar "Volver al mapa". Es el mismo problema de fondo, se resuelve junto con `SIG.Views`.

   **Ajustes pedidos por el usuario después de la primera prueba, aplicados y confirmados**:
   - Botón ribbon "Capas" (antes `alert("Capas")`) → ahora `SIG.LayerPanel.toggle()`, nueva función en `script_layerpanel.html` que muestra/oculta el panel flotante `#sig-layers-panel` completo (distinto del botón interno `btn-toggle-list`, que solo colapsa la lista dejando el header visible).
   - Botón ribbon "Zoom" (antes `alert("Zoom")`) → ahora `SIG.Layers.zoomAll()`, mismo comportamiento que el botón `btn-zoom-all` del panel flotante.
   - `script_ribbon_config.html` reorganizado: pestaña **Mapa** quedó solo con el grupo "Navegación" (Capas, Zoom); se agregó una pestaña **Datos** nueva con el grupo "Unidades" y el botón "Riego" (antes vivía como grupo dentro de la pestaña Mapa).
5. ✅ Filtros + tarjetas resumen dinámicas (portar patrón de `Version previa/js/filtros.js`). Decisión tomada con el usuario: viven en una **pestaña nueva "Resumen", independiente** de la pestaña "Datos" (no filtran la tabla de DataTables) — pide sus propios datos con `SIG_UD_get`, mantiene su propio estado de filtros. Evita necesitar estado compartido entre vistas por ahora. **Confirmado funcionando en el Apps Script real** (filtros y tarjetas se ven correctamente).
   Detalle:
   - `script_views.html` (nuevo) — módulo `SIG.Views`, reemplaza el toggle manual de `#map`/`#sig-table-view` que tenía `SIG.Table`: una lista fija de ids de vista (`map`, `sig-table-view`, `sig-summary-view`), `SIG.Views.show(id)` oculta todas menos la pedida. Es el primer paso del `SIG.Views` genérico que había quedado pendiente como deuda en el paso 4; se volvió necesario al sumar una tercera vista. `script_table.html` ya se actualizó para usarlo (`SIG.Views.show("sig-table-view")` / `SIG.Views.show("map")`).
   - `style_summary.html` / `view_summary.html` (nuevos) — panel `#sig-summary-view`, misma estructura que la tabla: header con título + "Volver al mapa", fila de filtros, fila de tarjetas.
   - `script_summary.html` (nuevo) — módulo `SIG.Summary`: filtros = un `<select>` por cada campo del esquema con `ui="SEL"` (poblado con valores únicos de esa columna); tarjetas = una de "Registros" (conteo) + una por cada campo con `estadistica="SUM"` o `"AVG"` (suma o promedio), todo recalculado sobre las filas que cumplen los filtros activos. Nada hardcodeado, todo sale del esquema de `CONFIGURACION_COLUMNAS`.
   - `script_ribbon_config.html` — nueva pestaña "Resumen" con botón "Riego" → `SIG.Summary.show("UD_RIEGOS")`.
   - `Index.html` — `include()` de los 4 archivos nuevos.
   **Para probar**: copiar `script_views.html`, `style_summary.html`, `view_summary.html`, `script_summary.html`, `script_table.html` (modificado), `script_ribbon_config.html` (modificado) e `Index.html` (modificado) al Apps Script real. Pestaña Resumen → botón Riego. Confirmar que los filtros desplegables y las tarjetas cambian según lo esperado, y que "Volver al mapa" funciona.

   **Bugs reportados en la primera prueba (2026-08-13) y ya corregidos**:
   1. Los botones "Riego" de Datos y de Resumen no cargaban nada — causa más probable: no se copiaron los 4 archivos **nuevos** al Apps Script (`script_views.html` en particular; si `SIG.Views` no existe, `SIG.Table.show`/`SIG.Summary.show` truenan en silencio en la consola del navegador, sin alert visible porque el error ocurre antes de llegar al `google.script.run`). No es un bug de lógica, es un archivo nuevo faltante en el proyecto de Apps Script — a diferencia de editar un archivo existente, crear uno nuevo requiere el paso extra de "+ → Script/HTML" en el editor.
   2. La pestaña "Mapa" del ribbon no volvía a mostrar el mapa — esto sí era un bug real: las pestañas del ribbon (`script_component_ribbon.html`) solo cambiaban qué botones se ven, nunca controlaron la vista principal (`#map`/`#sig-table-view`/`#sig-summary-view`). Corregido agregando un `onShow` opcional a `addTab()` — la pestaña "Mapa" ahora declara `onShow:()=>SIG.Views.show("map")` en `script_ribbon_config.html`, y `SIG.Ribbon.show()` lo llama al activarse la pestaña.
   **Para probar el fix**: copiar `script_component_ribbon.html` y `script_ribbon_config.html` (ambos modificados), además de re-verificar que los 4 archivos nuevos del punto anterior sí estén creados en el Apps Script (no solo editados). Si el botón Riego sigue sin hacer nada después de esto, abrir la consola del navegador (F12) en el web app y mandar el error exacto que aparezca ahí.
   3. `script_config.html` no existía en el Apps Script real (`Uncaught ReferenceError: loadConfig is not defined`) — era un archivo **original**, no creado en esta sesión, que aparentemente nunca se había copiado. Se resolvió creándolo con el contenido ya existente en el repo local. Quedó como aprendizaje: cuando aparece un `ReferenceError` de una función que sí existe en el código local, sospechar primero de un archivo faltante en Apps Script antes que de un bug de lógica.
   4. Con los archivos ya completos, los botones "Riego" seguían sin mostrar nada — se agregaron `console.log` con prefijo `[SIG.Table]`/`[SIG.Summary]`/`[SIG.Views]`/`[SIG.Ribbon]` en `script_table.html`, `script_summary.html`, `script_views.html`, `script_component_ribbon.html`, `script_ribbon_config.html` para diagnosticar. El log mostró que todo el flujo de datos funcionaba perfecto (32 columnas, 114 filas, sin errores) — el problema real era CSS: `SIG.Views.show()` ponía `el.style.display = ""` para "mostrar" una vista, pero `style_table.html`/`style_summary.html` tienen `display:none` puesto directamente en la hoja de estilos (no solo inline) — limpiar el estilo en línea con `""` no alcanza para ganarle a esa regla, la vista seguía oculta aunque los datos ya estuvieran cargados. Con `#map` no se notaba porque no tiene ningún `display:none` en su CSS. **Corregido** en `script_views.html`: ahora usa `"block"` explícito en vez de `""`.
   **Para probar el fix**: copiar `script_views.html` (con el fix de `"block"`, los `console.log` de diagnóstico se dejaron, no hacen daño y sirven para futuras dudas) y confirmar que Datos → Riego y Resumen → Riego ya muestran contenido visible.
   **Filtros en cascada (2026-08-20), a pedido del usuario**: quería que al elegir Región, Provincia/Municipio se acoten solo a los que corresponden (y viceversa). Evaluamos dos caminos: (A) basado en el catálogo `REGIONES`/`PROVINCIAS`/`MUNICIPIOS` — el usuario ya había agregado `region_id`/`Provincia_id` ahí — pero cruzarlo requeriría matchear por nombre el texto que cada fila UD_ ya trae (`"CONO SUR"` en UD_RIEGOS) contra el `nombre` del catálogo (`"Cono Sur"`), frágil por las inconsistencias de mayúsculas/tipeo ya documentadas; (B) **elegida** — sin depender de ningún catálogo, cada `<select>` calcula sus propias opciones a partir de las filas que ya cumplen los **demás** filtros activos (excluyéndose a sí mismo). Autoconsistente con lo que la propia hoja UD_ ya tiene, funciona en cualquier dirección (elegir Provincia primero también acota Región) y para cualquier combinación de campos `SEL`, no solo los geográficos.
   - `script_summary.html` — `uniqueValues(sourceRows, columna)` ahora recibe las filas sobre las que calcular, en vez de usar siempre todas; nueva `getRowsExcludingFilter(columnaExcluida)` (todas las filas que cumplen los filtros activos salvo el de esa columna — son las opciones válidas para su propio select). `buildFilterSelect` arma cada `<select>` con esas opciones y conserva el valor ya elegido (`select.value = activeFilters[...]`). `renderFilters()` ahora limpia primero cualquier filtro cuyo valor elegido ya no aparezca entre las opciones válidas (evita dejarlo "pegado" mostrando 0 resultados sin explicación) antes de dibujar los `<select>`. Cambiar cualquier filtro ahora vuelve a llamar `renderFilters()` (no solo `refresh()`), porque cambiar uno puede volver inválida la opción elegida en otro.
   **Para probar**: copiar `script_summary.html` (modificado). Pestaña Resumen → Riego → elegir una Región y confirmar que Provincia/Municipio solo muestran las que corresponden; probar también el camino inverso (elegir Municipio primero).
   **Bug reportado tras probar el cascada y ya corregido**: en Riegos, el filtro de `GESTION` volvía a "Todos" apenas se elegía un valor. Causa: `GESTION` es el único campo `SEL` de tipo `NUM` en Riegos (los demás son texto) — la fila trae `2019` como número, pero `select.value` siempre devuelve string (`"2019"`). El chequeo nuevo de "¿la selección elegida sigue siendo válida?" comparaba `opciones.indexOf(valorActivo)` sin pasar por `String()` en los dos lados (a diferencia de `getFilteredRows`/`getRowsExcludingFilter`, que sí lo hacían) — `"2019" !== 2019` invalidaba el filtro apenas se elegía. **Corregido**: la comparación ahora usa `String(opcion) === String(valorActivo)`.
   **Para probar el fix**: copiar `script_summary.html` (modificado) y confirmar que el filtro de Gestión en Riego ya conserva el valor elegido.
6. 🔶 Gráficas (barras/anillos) — paso intermedio que faltaba entre el 5 y el 6 original, detectado por el usuario: ya teníamos las tarjetas de indicadores pero no las gráficas de `Version previa/js/graficos.js`. Viven junto a los filtros y tarjetas en la pestaña **Resumen** (mismo módulo `SIG.Summary`, no una vista aparte).
   **Hecho, todavía no probado en el Apps Script real**:
   - `Index.html` — se agregó Chart.js 4.4.4 por CDN.
   - `view_summary.html` — nueva fila `#sig-summary-charts` debajo de las tarjetas.
   - `script_summary.html` — se agregó `getChartFields()` (campos del esquema con `grafico="BAR"` o `"PIE"`), `countByValue()` (cuenta filas por cada valor distinto de la columna, sobre las filas ya filtradas), `renderCharts()` (una tarjeta con `<canvas>` por campo, `Chart.js` tipo `bar` o `doughnut` según el esquema). Se agregó una guarda: si un campo tiene más de 15 valores distintos (o ninguno), se omite su gráfica en vez de dibujar algo ilegible — así los campos numéricos continuos con `GRAFICO=BAR` pero valores casi todos únicos (`COSTO_TOTAL`, `AREA_DE_RIEGO`, etc.) se quedan solo como tarjeta, sin gráfica de conteo sin sentido; los que sí tienen pocas categorías (`TIPO_DE_PROYECTO`, `ESTADO_DEL_PROYECTO`, `GESTION`) sí grafican. Se unificó `renderCards()`+`renderCharts()` en una función `refresh()`, usada en los 3 lugares donde antes se llamaba `renderCards()` sola (carga inicial, cambio de filtro, limpiar filtros).
   **Deuda reconocida**: las gráficas de barra hoy son "conteo de filas por valor", no "suma/promedio de un indicador agrupado por otra columna" (ej. "Costo total por Región", que sí tenía `Version previa`). Eso requeriría una columna de "agrupar por" en el esquema que no existe todavía — se deja pendiente, no bloquea nada de lo demás.
   **Para probar**: copiar `Index.html`, `view_summary.html`, `script_summary.html` (los 3 modificados) al Apps Script real. Pestaña Resumen → Riego, confirmar que aparecen gráficas de Tipo de proyecto, Estado y Gestión, y que cambian al aplicar filtros.
   **Ajuste pedido por el usuario tras confirmar el paso 6**: quería ver el botón de todas las unidades (`UD_RESIDUOS_SOLIDOS`, `UD_FORESTACIONES`, `UD_CUENCAS`, etc.), no solo "Riego" — aunque estén vacías. Antes el botón de "Riego" estaba hardcodeado a mano en `script_ribbon_config.html` porque era la única unidad con filas en `CONFIGURACION_COLUMNAS`. Cambiado a dinámico:
   - `115_sigUD.gs` — nueva `SIG_UD_listUnidades()`: lista toda hoja `UD_*` que exista de verdad en el spreadsheet (`SIG_getSpreadsheet().getSheets()`), con una etiqueta derivada del nombre (`UD_RESIDUOS_SOLIDOS` → "Residuos Solidos"). No depende de `CONFIGURACION_COLUMNAS` — aparece aunque la unidad no tenga ninguna columna configurada todavía.
   - `script_ribbon_config.html` — ya no hardcodea el botón "Riego"; pide `SIG_UD_listUnidades()` y arma un botón por unidad en Datos y en Resumen. Como la respuesta llega async (después de que el ribbon ya se dibujó), se agregó `SIG.Ribbon.refresh()` (nuevo en `script_component_ribbon.html`) para redibujar la pestaña actual si el usuario ya la tenía abierta cuando llegan los botones.
   - `script_table.html` / `view_table.html` — si una unidad no tiene columnas en `CONFIGURACION_COLUMNAS` (esquema vacío), ya no se intenta armar un DataTable sin columnas (rompía) — se muestra un aviso ("Esta unidad todavía no tiene columnas configuradas") en `#sig-table-vacio` en su lugar.
   - `SIG.Summary` no necesitó guarda: con esquema vacío simplemente no dibuja filtros/tarjetas de indicador/gráficas, pero la tarjeta "Registros" (conteo) sí se sigue mostrando siempre.
   - Nota sobre unidades sin la hoja creada todavía (si alguna de `UD_CUENCAS`/`UD_GESTION_RIESGOS`/`UD_FISCALIZACION`/`UD_BIODIVERSIDAD` no existe como pestaña real en el spreadsheet): esa unidad simplemente no aparece en la lista de botones — `SIG_UD_listUnidades()` solo lista hojas que existen de verdad. Si alguna sí existe pero vacía (sin filas), sí aparece y muestra tabla/resumen con 0 registros.
   **Para probar**: copiar `115_sigUD.gs`, `script_ribbon_config.html`, `script_component_ribbon.html`, `script_table.html`, `view_table.html` (los 5 modificados) al Apps Script real. Confirmar que aparece un botón por cada unidad real del spreadsheet en Datos y en Resumen, y que las que no tienen `CONFIGURACION_COLUMNAS` muestran el aviso en vez de romperse.
   **Bugs reportados tras ese ajuste y ya corregidos**:
   1. Etiquetas de dos palabras (`"Residuos Solidos"`, `"Gestion Riesgos"`) se salían de su botón e invadían al vecino — `.ribbon-button` no tenía manejo de corte de palabra ni texto centrado. **Corregido** en `component_ribbon.html`: `width` de 70 a 80px, `text-align:center`, y la etiqueta ahora tiene su propio estilo (`font-size:.72rem`, `overflow-wrap:break-word`, `white-space:normal`) para que las palabras largas corten en vez de desbordar.
   2. Pedido del usuario: un indicador de carga/error reutilizable — hasta ahora, mientras esperaba la respuesta de `google.script.run` no había ninguna señal visible, y un error se perdía en la consola o en un `alert()` bloqueante. **Nuevo módulo `SIG.Loading`** (`component_loading.html` + `script_component_loading.html`, mismo patrón que `component_ribbon`/`script_component_ribbon`): una franja fija bajo el ribbon (`#sig-loading`) con 3 métodos — `show(texto)` (spinner + texto), `error(texto)` (ícono de alerta + texto, fondo rojo, queda visible hasta el próximo `show`/`hide`), `hide()`. Conectado en los 3 lugares que piden datos por `google.script.run`: `script_table.html`, `script_summary.html` y la carga de unidades en `script_ribbon_config.html`. Se reemplazaron los `alert()` de error de `script_table.html`/`script_summary.html` por `SIG.Loading.error(...)` (menos intrusivo, mismo lugar que el estado de carga). De paso se corrigió un bug relacionado: si `SIG_UD_get` fallaba, `currentHoja` había quedado seteado igual — un reintento (clickear el mismo botón de nuevo) no volvía a pedir los datos por el chequeo de "ya estaba cargada"; ahora el error resetea `currentHoja = null`.
   **Para probar**: copiar `component_ribbon.html` (modificado), `component_loading.html` y `script_component_loading.html` (nuevos), `Index.html`, `script_table.html`, `script_summary.html`, `script_ribbon_config.html` (modificados) al Apps Script real.
7. ✅ Vista Mapa: cruzar `UD_*.SIG_GEOMETRIAS_ID` → `SIG_GEOMETRIAS` para ubicar cada registro. El mapa ya cargaba todas las geometrías de `SIG_GEOMETRIAS` desde el inicio (`SIG.Features`), pero sin ningún dato de a qué proyecto pertenecía cada una — el cruce que faltaba es justamente ese, no un selector de unidad aparte en el mapa. **Confirmado funcionando en el Apps Script real**: clickear un punto muestra los detalles del proyecto/botadero al que corresponde.
   **Corrección (2026-08-31, encontrado por el Homúnculo Soberbia)**: el diagnóstico original de por qué `UD_FORESTACIONES` no mostraba detalle ("no tiene fila en `CONFIGURACION_COLUMNAS` aún, esperado") **era incorrecto** — la causa real es que esa hoja usa la columna `SIG_GEOMETRIA_ID` (singular), no `SIG_GEOMETRIAS_ID` (plural) como el resto de las unidades. `SIG_UD_indiceGeometrias` busca por el nombre plural, así que las filas de Forestaciones nunca entraban al índice — y seguirá sin funcionar aunque se le agregue configuración, hasta corregir el nombre de columna en la hoja real (decisión pendiente del usuario, es una hoja con datos ya cargados).
   **Hecho, todavía no probado en el Apps Script real**:
   - `115_sigUD.gs` — nueva `SIG_UD_indiceGeometrias()`: recorre todas las hojas `UD_*` (via `SIG_UD_listUnidades()`), y arma un índice `SIG_GEOMETRIAS_ID → {hoja, etiqueta, campos}` con los campos marcados `VISIBLE_MAPA=true` del esquema de cada unidad (mismo patrón que `VISIBLE_TABLA` para la tabla). Reutiliza `SIG_UD_getSchema`/`SIG_UD_getRows` ya cacheados, no agrega una cache nueva.
   - `120_sig_geometry.gs` — nueva `SIG_Geometrias_listConRegistros()`: la lista de `SIG_Geometrias_list()` de siempre, pero con un campo `registro` agregado a cada geometría (`null` si ninguna fila UD_ la referencia — geometría suelta).
   - `script_main.html` — `initApp()` ahora pide `SIG_Geometrias_listConRegistros()` en vez de `SIG_Geometrias_list()`. De paso se le agregó `SIG.Loading`/manejo de error, que no tenía (la única llamada de `google.script.run` en toda la app sin `withFailureHandler`).
   - `script_features.html` — `SIG.Features.load()` ahora arma un popup (`layer.bindPopup(...)`) con la etiqueta de la unidad + sus campos `VISIBLE_MAPA` cuando la geometría tiene `registro`; si no tiene registro cruzado, cae a mostrar `nombre`/`descripcion` propios de `SIG_GEOMETRIAS` si existen. Los valores se escapan (`escapeHtml`) antes de meterlos al HTML del popup — vienen de celdas de la hoja, no hay que confiar en ellas a ciegas.
   **Deuda reconocida**: hoy la única forma de "ubicar" un registro es clickear su geometría en el mapa y ver el popup — no hay todavía un botón "ver en el mapa" desde una fila de la tabla/resumen que haga zoom automático a esa geometría. Queda pendiente si hace falta.
   **Para probar**: copiar `115_sigUD.gs`, `120_sig_geometry.gs`, `script_main.html`, `script_features.html` (los 4 modificados) al Apps Script real. Clickear un punto/geometría del mapa que pertenezca a `UD_RIEGOS` y confirmar que el popup muestra Proyecto/Región/Provincia/Municipio/Estado.
   **Limpieza pedida por el usuario tras confirmar este paso**: `escapeHtml` (en `script_features.html`) y `formatNumber` (en `script_summary.html`) eran helpers chicos escritos cada uno en su propio módulo — antes de que se repitiera un tercero, se sacaron a un módulo compartido `SIG.Utils` (`script_utils.html`, era un stub HTML vacío sin usar, ahora con contenido real). `script_features.html`/`script_summary.html` ahora solo hacen `const escapeHtml = SIG.Utils.escapeHtml` / `const formatNumber = SIG.Utils.formatNumber`. `Index.html` incluye `script_utils` justo después de `script_main`, antes que cualquier otro módulo que lo use.
   **Para probar**: copiar `script_utils.html` (nuevo/reescrito), `Index.html`, `script_features.html`, `script_summary.html` (modificados) al Apps Script real. No debería cambiar nada visible — es solo la refactorización.
   **Bug reportado (2026-08-20) y ya corregido**: al cambiar de hoja en la tabla, DataTables tiraba `"Cannot reinitialise DataTable"` y la tabla quedaba con contenido pisado/mezclado en vez de limpio. Dos causas atacadas en `script_table.html`:
   1. Intento 1 (insuficiente): sumar `destroy:true` a las opciones de init. Esto en realidad **causó un segundo bug** — `"Incorrect column count"` al pasar de Riego a Residuos Sólidos —, porque `destroy()` (manual o vía `destroy:true`) restaura el `<thead>` a como estaba en la inicialización **original**, no al estado actual; como `buildHead()` ya había reemplazado esos `<th>` por los nuevos, el destroy interno los pisaba de vuelta a los viejos justo antes de aplicar el `columns` nuevo → conteo desincronizado.
      **Corregido de raíz**: en vez de reusar el mismo `<table id="sig-table-datatable">` entre renders (destroy + reconstruir thead + reinit, con todas las referencias viejas de DataTables dando vueltas), ahora `resetTableElement()` reemplaza el `<table>` entero por uno limpio en cada render — DataTables nunca vio ese nodo antes, no hay registro previo con el que chocar. Se sacó el `destroy:true` (ya no hace falta, nunca hay una instancia viva en el nodo nuevo); se mantiene el `dataTable.destroy()` manual sobre la instancia vieja antes de reemplazar el nodo, por prolijidad (libera su registro interno antes de descartarla).
   2. Bug relacionado encontrado de paso (no confirmado como la causa exacta, pero sí un problema real): si el usuario cambiaba de hoja rápido, antes de que la respuesta anterior llegara, una respuesta vieja podía llegar después de la nueva y pisarla — `SIG_UD_get(hoja)` no devuelve de qué hoja es la respuesta, así que no había forma de detectar esto. Corregido capturando la `hoja` pedida en el `withSuccessHandler` (`resultado => render(hoja, resultado)`) y comparándola contra `currentHoja` al llegar — si ya cambió, la respuesta vieja se descarta. Mismo fix aplicado en `script_summary.html` por el mismo riesgo, aunque ahí no usa DataTables.
   **Para probar**: copiar `script_table.html`, `script_summary.html` (modificados). Cambiar de hoja varias veces seguidas (Riego → Residuos Sólidos → Riego) en Datos y en Resumen, rápido y despacio, confirmar que no aparece el warning y que la tabla/resumen siempre corresponde a la última hoja clickeada.
8. 🔶 **CRUD real** (2026-08-20, adelantado antes que reportes a pedido del usuario). Decisión tomada con el usuario: por ahora los formularios de alta/edición leen los campos de `CONFIGURACION_COLUMNAS` (mismo patrón que tabla/filtros/gráficas) — la "libertad absoluta" de escribir en unidades sin configurar, o de agregar columnas nuevas sin tocar `CONFIGURACION_COLUMNAS`, queda para más adelante a propósito.
   Antes de escribir código se relevó qué había armado ya (Dibujar→Modal→`SIG.Geometry.build`): todo el flujo de UI ya existía pero terminaba en `console.log(geo)` — cero funciones de escritura en el backend, sin `LockService`, sin estrategia de ID, `id` siempre `null` incluso al editar.
   **Backend (genérico, reutilizable para cualquier hoja)**:
   - `110_sigConfig.gs` — `SIG_saveRow(sheetName, datos)`: inserta o actualiza una fila por nombre de columna (no por posición), dentro de un `LockService.getScriptLock()` para que dos guardados a la vez no pisen el mismo ID. Actualiza solo las columnas presentes en `datos` (deja el resto tal cual); si no viene `datos.ID`, calcula el próximo ID (`max(ID existente)+1`, o `1` si la hoja está vacía) e inserta.
   - `115_sigUD.gs` — `SIG_UD_saveRow(hoja, datos)`: wrapper sobre `SIG_saveRow` que solo deja escribir columnas que ya están en el esquema de esa unidad (evita basura en columnas no configuradas — es la restricción explícita de esta etapa), e invalida la cache (`SIG_Cache_remove("ud_rows_"+hoja)`) después de guardar.
   - `120_sig_geometry.gs` — `SIG_Geometrias_save(geo)` (inserta/actualiza) y `SIG_Geometrias_delete(id)` (baja lógica, pone `ELIMINADO=true`). De paso, `SIG_Geometrias_list()` ahora filtra las eliminadas — antes no lo hacía, así que una eliminación no se hubiera notado en el mapa.
   - `115_sigUD.gs` — `SIG_UD_indiceGeometrias()` ahora también devuelve `registroId` (el `ID` de la fila UD_, no solo su hoja/etiqueta/campos) — hacía falta para poder preseleccionar el registro vinculado al editar una geometría existente.
   **Frontend — CRUD de geometrías** (`script_geometry.html` reescrito):
   - `build()` corregido: ahora manda `id: layer.geometriaId || null` (antes siempre `null`, ni siquiera en modo editar — no había forma de distinguir crear de actualizar).
   - `save(layer, form)`: guarda la geometría y, si se eligió una unidad+registro en el formulario, vincula (`SIG_UD_saveRow` con `SIG_GEOMETRIAS_ID`). Actualiza `layer.geometriaId`/`layer.geometriaData` al terminar, para que un segundo editar en la misma sesión ya muestre lo recién guardado.
   - `remove(layer)`: baja lógica + saca el layer del mapa (por las dos vías posibles: `SIG.Features.remove` para geometrías cargadas del mapa al inicio, `SIG.Map.get().removeLayer(layer)` como respaldo para una recién dibujada en la misma sesión que `SIG.Features` nunca llegó a registrar).
   - `setupLinkFields(form, seleccionActual)`: arma el `<select>` de unidad (`SIG_UD_listUnidades`) y, en cascada, el de registro de esa unidad (`SIG_UD_get`), usando como etiqueta el campo de menor `ORDEN` del esquema (mismo criterio ya usado a mano para `NOMBRE_DEL_PROYECTO`/`BOTADERO` — sin hardcodear qué columna es "el nombre" por unidad).
   - `prefill(form, item)` — precarga nombre/descripción/capa/estilo/estado + muestra el botón Eliminar, en modo edición.
   - `view_geometry.html` — nueva sección "Registro relacionado (opcional)" (selects de unidad/registro) y botón Eliminar (oculto por defecto, solo aparece en modo edición).
   - `script_selection.html`/`script_draw.html` — conectados a lo de arriba vía dos hooks nuevos en `SIG.Modal` (`onOpen`, corre apenas el componente ya está en el DOM — para precargar/armar selects; `onSaved`, corre después de un guardado exitoso). `SIG.Modal` ahora también espera a que `onSave` termine (puede ser async) antes de cerrarse — si falla, se queda abierto con el error visible en vez de cerrar igual.
   - `script_features.html` — `SIG.Features.load()` ahora le pega a cada layer cargado `geometriaId`/`geometriaData` y lo registra en `SIG.Selection` — antes solo las geometrías recién dibujadas en la sesión eran seleccionables/editables por doble clic; las que ya estaban en el mapa desde el arranque no lo eran.
   **Frontend — CRUD de registros** (nuevo, independiente del de geometrías — se puede cargar un registro sin ubicarlo en el mapa, y viceversa):
   - `view_record.html` + `script_record.html` (nuevos) — módulo `SIG.Record`: arma un formulario a partir de `SIG_UD_getSchema(hoja)` (un campo por columna, tipo de `<input>` según `TIPO`: número/fecha/texto), excluyendo la clave primaria y las columnas de sistema (`SIG_GEOMETRIAS_ID`, `DOCUMENTOS_ID`). `SIG.Record.open(hoja, registro, onSaved)` sirve tanto para alta (`registro` vacío) como edición (precarga los valores). El botón Guardar queda deshabilitado mientras se piden los campos, para que no se pueda guardar antes de que existan.
   - `view_table.html` — nuevo botón "Nuevo registro" en el header.
   - `script_table.html` — nueva columna de acción (lápiz) al final de cada fila, delegada sobre `#sig-table-contenido` (no sobre la `<table>` misma, que se reemplaza en cada cambio de hoja); nueva `reload()` para refrescar la tabla después de guardar sin pasar por el chequeo de "ya estaba cargada" de `show()`.
   - `script_modal.html` — botón Guardar genérico: se renombró `#btn-save-geometry` → `#btn-modal-save` (el modal ya no es solo para geometrías).
   **Decisiones de alcance, explícitas para no asumirlas en silencio**:
   - Los campos del formulario de registro son siempre `<input>` simples — no hay todavía un `<select>` con los valores ya existentes para campos categóricos (`UI=SEL`). Queda para la etapa de "libertad absoluta"/pulido de UX.
   - ~~Los `<select>` de Capa/Estilo en el formulario de geometría siguen hardcodeados~~ — corregido, ver revisión Padre/Homúnculos más abajo.
   - No hay borrado de registros UD_ (solo de geometrías, que sí tienen `ELIMINADO`). Ninguna de las dos unidades configuradas (`UD_RIEGOS`, `UD_RESIDUOS_SOLIDOS`) tiene columna `ELIMINADO` en sus datos reales — y el caso de uso real ("dar de baja un proyecto") ya lo cubre cambiar `ESTADO_DEL_PROYECTO`, que es una edición normal. Si hace falta borrado real de todos modos, avisar.
   - La vista Resumen no tiene botones de alta/edición (solo Tabla) — decisión de mantener el alcance acotado, no un descuido.
   **Para probar**: copiar los 12 archivos tocados (`110_sigConfig.gs`, `115_sigUD.gs`, `120_sig_geometry.gs`, `script_geometry.html`, `view_geometry.html`, `script_selection.html`, `script_draw.html`, `script_features.html`, `script_modal.html`, `script_table.html`, `view_table.html`, `script_utils.html`) más los 2 nuevos (`view_record.html`, `script_record.html`) y el `Index.html` actualizado. Probar: (1) Datos → Riego → "Nuevo registro", cargar un proyecto, Guardar, confirmar que aparece en la tabla. (2) Editar ese registro desde el lápiz de su fila. (3) Dibujar un punto en el mapa, en el modal vincularlo a ese registro nuevo, Guardar, confirmar que el punto aparece con popup. (4) Doble clic sobre una geometría ya existente en el mapa (cargada desde el arranque) — debería abrir el modal precargado y permitir editar/eliminar.
   **Bug reportado (2026-08-20) y ya corregido, sin relación con el CRUD**: al probar, dejó de cargar el mapa base (Municipios/Provincias/Regiones) — `Exception: ID de archivo o carpeta no válido: 5`. Causa real: `SIG_Config_getLayerConfig` (`110_sigConfig.gs`) leía las columnas de `REGIONES`/`PROVINCIAS`/`MUNICIPIOS` por **posición** (`row[2]` = "el tercer valor de la fila" = `file_id`) — al agregarle `region_id` a `PROVINCIAS` y `Region_id`/`Provincia_id` a `MUNICIPIOS` (para el filtro en cascada, unas conversaciones atrás), todas las columnas de después se corrieron un lugar (dos en Municipios) — `row[2]` pasó a ser el valor de `region_id` (un número chico como "5") en vez del ID real de Drive. **Corregido**: ahora usa `SIG_getRows(sheetName)` (lectura genérica por nombre de columna, la misma que ya usa todo lo demás) en vez de leer por posición — agregar columnas nuevas en cualquier lugar de esas hojas ya no puede volver a romper esto.
   **Para probar el fix**: copiar `110_sigConfig.gs` y confirmar que el mapa base (capas de Municipios/Provincias/Regiones) vuelve a cargar.
   **Bug reportado y ya corregido**: el modal de "Nuevo registro" se salía de la pantalla — `.sig-modal-content` (`style_modal.html`) tenía ancho fijo de `420px` sin scroll, pensado para el formulario chico de geometría (5 campos); el de registro puede tener 15-20+. A esos `420px` ni siquiera se llegaba al punto de quiebre de Bootstrap para 2 columnas (`col-md-*` necesita ≥768px), así que cada campo ocupaba una fila entera. **Corregido**: el modal ahora es `width:90vw; max-width:960px; max-height:85vh` con `display:flex` (header fijo, `#sig-modal-body` como único que hace scroll interno si el contenido no entra); de paso `script_record.html` pasó de `col-md-6` (2 columnas) a `col-md-4` (3 columnas), que ahora sí se activa. Afecta también al modal de geometría (mismo estilo compartido) — ahí no hay problema, antes ya cabía, ahora tiene más aire.
   **Para probar el fix**: copiar `style_modal.html` y `script_record.html`, confirmar que "Nuevo registro" se ve en 3 columnas y entra en la pantalla (con scroll si hiciera falta).
   **Bug reportado y ya corregido**: doble clic para editar un punto/área ya existente en el mapa (no una recién dibujada) → al Guardar, `TypeError: Cannot read properties of undefined (reading 'type')` en `getType`. Causa: una geometría recién dibujada (Leaflet.Draw) es un layer suelto — `layer.toGeoJSON()` da un `Feature` normal. Pero una geometría **cargada del mapa** (`SIG.Features.load`) viene envuelta en `L.geoJSON(feature,...)`, que Leaflet trata como un grupo — su `.toGeoJSON()` devuelve una `FeatureCollection` (sin `.geometry` propio en el nivel de arriba), aunque adentro solo tenga una geometría. `getType`/`build` asumían siempre el primer caso. **Corregido**: nueva `toFeature(layer)` en `script_geometry.html` que normaliza los dos casos (si es `FeatureCollection`, toma `.features[0]`) antes de leer `.geometry`.
   **Para probar el fix**: copiar `script_geometry.html`. Doble clic sobre un punto/área que ya estaba en el mapa desde el arranque (no uno recién dibujado en esta sesión), editar algo y Guardar.

   **Revisión Padre/Homúnculos (2026-08-31) — primera prueba real del sistema de agentes en este proyecto.** Se pidió a Padre triage sobre "revisar el estado del proyecto"; convocó a Ira, Soberbia y Avaricia en paralelo (cada uno corrió una vez, no anidado — Padre ya no tiene la tool `Agent`, solo devuelve el plan de a quién convocar). Encontraron, con evidencia verificada contra datos reales (copia local del spreadsheet) y contra el DOM (Ira corrió jsdom):

   - **Corrupción activa confirmada en datos reales**: al editar una geometría existente, `TIPO_ID` se pisaba con el string de GeoJSON (`"Point"`) en vez del FK numérico a `SIG_TIPO`, y `ESTADO` quedaba vacío o mal escrito — confirmado en la fila real `ID=73` de `SIG_GEOMETRIAS` tras una prueba del usuario.
   - `getLabelField` (vínculo geometría↔registro) devolvía `"ID"` en vez del nombre del proyecto — el `<select>` de "Registro" mostraba números pelados, inusable.
   - Vincular una geometría a un registro no desvinculaba la fila anterior — dos registros podían reclamar la misma geometría (confirmado con datos reales: 4 colisiones ya existentes entre `UD_RIEGOS`/`UD_FORESTACIONES`, enmascaradas porque Forestaciones usa `SIG_GEOMETRIA_ID` singular, ver corrección más arriba).
   - `script_record.html` estaba **completamente roto en `UD_RIEGOS`** (la única unidad con datos reales importantes): dos columnas reales tienen paréntesis/espacio en el nombre (`EJECUCION_FISICA (%)`, `EJECUCION_FINANCIERA(%)`), que rompían el selector CSS del formulario (`DOMException`, verificado en jsdom) — en modo editar dejaba el botón Guardar deshabilitado para siempre a mitad de carga; en modo nuevo registro, guardar no hacía nada visible.
   - Detrás de ese bug, uno más: editar cualquier registro de Riego vaciaba las columnas de fecha (`ORDEN_DE_PROCEDER`, `FECHA_DE_CONCLUCION`, 64 filas reales) — un `input[type=date]` solo acepta `"YYYY-MM-DD"`, y lo que llegaba del backend (objeto `Date` o ISO completo) no calzaba, se guardaba `""`.
   - Texto multilínea (`OBSERVACIONES`, 26 filas) perdía los saltos de línea al pasar por un `<input>` en vez de `<textarea>`.
   - Doble clic en Guardar creaba dos filas/geometrías (sin guarda de reentrada).
   - Hallazgo de seguridad, aparte: no hay ninguna verificación de usuario/rol en el backend — toda función `.gs` es invocable directo desde la consola del navegador. Evaluado con el usuario: por ahora solo él tiene el link de despliegue, así que queda para cuando el acceso se abra a más gente — no se toca en esta pasada.

   Aclarado con el usuario antes de corregir: una geometría es de **un solo registro**, nunca varios (decisión explícita, no ambigüedad técnica); `SIG_TIPO` se mantiene viva (hay que mapear el tipo de GeoJSON al FK real, no dejarlo como string); `ESTADO` en `SIG_GEOMETRIAS` no es "activo/inactivo" tipeable — es si está vinculada o no a un registro, así que se deriva, no se guarda; `ELIMINADO` es la papelera real (ya estaba bien). La hoja `CONFIGURACION` (el diccionario de códigos que nadie leía) ya no existe en el spreadsheet — el usuario la sacó, no queda nada que decidir ahí.

   **Corregido**:
   - `120_sig_geometry.gs` — nueva `SIG_Tipo_getIdPorNombre()`, mapea el tipo de GeoJSON al ID real de `SIG_TIPO` antes de guardar (en los 174 registros reales nunca se usó nada más que POINT, así que el mapeo simple por nombre alcanza). `SIG_Geometrias_save` ya no escribe `ESTADO`.
   - `115_sigUD.gs` — `SIG_UD_indiceGeometrias` ahora loguea (no oculta) una colisión si dos filas reclaman la misma geometría; nueva `SIG_UD_vincularGeometria(geometriaId, hoja, registroId)`, que desvincula cualquier otra fila que reclame esa geometría antes de vincular la nueva (o solo desvincula, si se elige "Sin vincular") — la exclusividad ya no depende de que el usuario no cometa un error de UI.
   - `script_geometry.html` — `getLabelField` excluye la clave primaria; nueva `setupCapaEstiloFields()` puebla Capa/Estilo desde `SIG_Capas_list`/`SIG_Config_getStyles` reales en vez de las 4 opciones hardcodeadas que no coincidían con la hoja; el badge del modal ahora muestra "Vinculada: [proyecto]" / "Sin vincular" derivado del cruce, no un `ESTADO` tipeado.
   - `view_geometry.html` — se sacó el `<select>` de Estado (ya no es un dato editable).
   - `script_record.html` — nueva `campoId()` sanea el nombre de columna antes de usarlo como id CSS; `TEXTO` ahora arma `<textarea>` en vez de `<input>` (no pierde saltos de línea); nueva `toDateInputValue()` convierte `Date`/ISO a `"YYYY-MM-DD"` para precargar, y un campo `FECHA` vacío no se manda al guardar (evita pisar con `""` una fecha existente); el armado+precarga de campos quedó en su propio `try/finally` para que el botón Guardar nunca quede deshabilitado para siempre.
   - `script_modal.html` — el botón Guardar se deshabilita mientras la promesa de guardado está en vuelo (evita duplicados por doble clic).
   - `script_main.html` — invalidación de cache del esquema al correr el setup (`099_Setup.gs`, ver corrección más arriba); `SIG_Geometrias_list` ahora blinda el `JSON.parse` del GEOJSON (una celda corrupta ya no tira abajo la carga completa del mapa).
   - Dos líneas de este mismo README que el código ya desmentía (cache del esquema, diagnóstico de Forestaciones) — corregidas en los pasos 2 y 7 de arriba.

   **Decisión pendiente, sin tocar**: las 4 colisiones geometría↔registro y las 4 filas con `ID=1` en `UD_FORESTACIONES` eran datos de prueba (el usuario va a cargar los datos reales de esa unidad después, no hace falta migrar nada); el rename `SIG_GEOMETRIA_ID`→`SIG_GEOMETRIAS_ID` en la hoja real de Forestaciones sigue pendiente (recién entonces el cruce va a funcionar ahí); seguridad de acceso al backend (queda para cuando se abra el acceso a más usuarios); rendimiento (Avaricia midió que el caché de 100KB de `CacheService` no alcanza para `UD_RIEGOS`/`UD_FORESTACIONES`, y que `SIG_UD_indiceGeometrias` lee más columnas de las que usa — no bloquea nada hoy, queda para cuando escale).

   **Para probar**: copiar `120_sig_geometry.gs`, `115_sigUD.gs`, `script_geometry.html`, `view_geometry.html`, `script_record.html`, `script_modal.html`, `script_main.html`, `099_Setup.gs` (los 8 modificados). Reparar a mano la fila `ID=73` de `SIG_GEOMETRIAS` (poner `TIPO_ID=1`, dejar `ESTADO` como estaba antes de la prueba). Probar: (1) editar un registro de Riego con fecha y observaciones — confirmar que no se vacían. (2) editar una geometría existente — confirmar que `TIPO_ID` queda numérico. (3) vincular una geometría ya vinculada a otro registro — confirmar que se desvincula la anterior. (4) el `<select>` de Capa/Estilo debería mostrar los nombres reales, no "Proyectos"/"Proyecto/Municipio/Riesgo".
9. 🔶 Capas de datos en el mapa por unidad (2026-08-31, a pedido del usuario tras cargar Forestación: con 3 unidades de geometrías en el mapa a la vez ya no se distinguía nada). Decisión tomada con el usuario (pregunta directa, no asumida): un botón chico por unidad en el ribbon (pestaña Mapa) prende/apaga esa unidad entera; los filtros por campo viven en el panel flotante que ya existía (el mismo de Municipios/Provincias/Regiones), no en un menú nuevo colgando del botón — reusa la UI que ya había en vez de inventar un patrón nuevo.
   - `script_features.html` — nueva `SIG.Features.all()` (antes no había forma de listar todas las geometrías cargadas desde afuera del módulo).
   - `script_datalayers.html` (nuevo) — módulo `SIG.DataLayers`: `toggleUnidad(hoja)` prende/apaga todas las geometrías de esa unidad (`map.addLayer`/`removeLayer` sobre lo que ya está en `SIG.Features`, sin volver a pedir nada al backend); `setFiltro(hoja, etiqueta, valor)` filtra dentro de una unidad prendida, usando los campos que ya vienen en `item.registro.campos` (los `VISIBLE_MAPA` del cruce del paso 7) — no hizo falta pedirle nada nuevo al backend. Se salta el primer campo (el de menor `ORDEN`, la identidad del registro) como filtro, porque tiene casi tantos valores distintos como filas.
   - `view_layers.html` — nueva sección "Datos" en el panel flotante existente (se envolvieron los dos paneles en un `#sig-map-panels` común para que se apilen solos sin pisarse); oculta por completo cuando ninguna unidad prendida tiene filtros que mostrar.
   - `script_ribbon_config.html` — nuevo grupo "Datos" en la pestaña Mapa, un botón por unidad (misma lista de `SIG_UD_listUnidades()` que ya se pedía para Datos/Resumen, no un pedido nuevo).
   - `script_main.html` — el panel de filtros se dibuja una vez cuando terminan de cargar las geometrías del arranque.
   **Deuda reconocida**: el botón del ribbon no cambia de aspecto al prender/apagar (sin estado visual "presionado") — el mapa mismo (geometrías apareciendo/desapareciendo) y el panel de filtros son la señal hoy; una geometría **recién dibujada** en la sesión (antes de guardarla) no pasa por `SIG.Features`, así que el toggle de su unidad no la afecta hasta que se recargue la página — caso borde, no se resolvió.
   **Ajustes pedidos por el usuario tras la primera prueba**:
   1. El panel flotante (`#sig-map-panels`, Capas + Datos) quedaba visible encima de la Tabla/Resumen al cambiar de pestaña — no estaba enganchado a `SIG.Views`, que es lo que controla qué vista se ve. **Corregido**: `script_views.html` ahora tiene un concepto de "dependientes" — elementos que no son una vista en sí pero se muestran/ocultan junto con una — `#sig-map-panels` se registra como dependiente de `"map"`.
   2. El panel "Datos" no se podía colapsar, a diferencia del de Capas.
   3. Simplificar: no repetir la estructura por unidad, y sumar las unidades a la lista existente en vez de una tarjeta aparte; y que los botones del ribbon cambien de color cuando están prendidos.
   **Corregido, los tres juntos** (2 y 3 se resolvieron con el mismo cambio: fusionar los dos paneles en uno):
   - `view_layers.html` — se sacó la tarjeta separada `#sig-datalayers-panel`; ahora hay un solo `#sig-panel-colapsable` dentro de la MISMA tarjeta de Capas, con la lista de unidades ("Datos") debajo de la lista de capas — un solo botón de colapsar para todo el panel (`script_layerpanel.html` ajustado).
   - `script_datalayers.html` — `renderPanel()` (bloque de título + selects apilados, uno por unidad) reemplazado por `render()`/`buildRow()`: una fila por unidad, mismo lenguaje visual que ya usaba Capas (check + nombre) — un solo `buildRow()` genérico, no una plantilla por tabla. Los filtros de esa unidad quedan plegados atrás de un botón "embudo" en la fila, en vez de mostrarse siempre.
   - `script_component_ribbon.html` — un botón ahora puede declarar `isActive()` (opcional, no rompe los botones que no lo usan) — se vuelve a consultar cada vez que el botón se dibuja, así que el color se mantiene aunque se cambie de pestaña y se vuelva. `component_ribbon.html` tiene el estilo `.ribbon-button.active` (azul). El botón de cada unidad en Mapa → Datos ahora usa `isActive: () => SIG.DataLayers.getEstado(u.hoja).visible`.
   **Para probar**: copiar `script_views.html`, `view_layers.html`, `script_datalayers.html`, `script_layerpanel.html`, `script_component_ribbon.html`, `component_ribbon.html`, `script_ribbon_config.html`, `script_main.html` (los 8). Cambiar de Mapa a Datos/Resumen y volver — el panel debería desaparecer y reaparecer. Un solo panel "Capas y datos", con Datos debajo de la lista de capas — apagar una unidad ahí (o desde el botón del ribbon) debería sacar sus geometrías del mapa y pintar el botón del ribbon de azul cuando está prendida.

**Borrado de registros en la Tabla** (2026-09-02, a pedido del usuario — "por ahora solo tiene edición"). Baja lógica, mismo patrón que ya usan las geometrías (`ELIMINADO`), no borrado físico de la fila.
- `099_Setup.gs` — se agregó la columna `ELIMINADO` a `CONFIGURACION_COLUMNAS` para las 3 unidades (`UD_RIEGOS`, `UD_RESIDUOS_SOLIDOS`, `UD_FORESTACIONES`), oculta (`HID`).
- `115_sigUD.gs` — `SIG_UD_getRows` ahora filtra las filas con `ELIMINADO` (igual que `SIG_Geometrias_list` ya hacía) — desaparecen de tabla, resumen y mapa a la vez, sin tocar nada en esos otros módulos. Nueva `SIG_UD_delete(hoja, id)`: valida explícito que la unidad tenga `ELIMINADO` en el esquema antes de intentarlo — si no, tira error en vez de "guardar" en silencio sin tocar nada (`SIG_UD_saveRow` descarta claves que no están en el esquema).
- `script_table.html` — botón de basurero al lado del lápiz en cada fila, con `confirm()` antes de eliminar; recarga la tabla al terminar.
- `script_record.html` — `ELIMINADO` se excluye del formulario de alta/edición (es de sistema, no se tipea a mano).

**⚠️ Paso manual obligatorio, no opcional**: agregar el encabezado `ELIMINADO` (una columna vacía alcanza) a las hojas reales `UD_RIEGOS`, `UD_RESIDUOS_SOLIDOS` y `UD_FORESTACIONES` — no solo a `CONFIGURACION_COLUMNAS`. Si falta en la hoja real, `SIG_saveRow` ignora esa columna en silencio (bug ya documentado arriba, todavía sin arreglar en el motor genérico): el botón Eliminar no tira ningún error, pero el registro **no desaparece**, porque nunca se llegó a escribir nada. Confundiría muchísimo si no se hace este paso primero.

**Para probar**: agregar la columna `ELIMINADO` a las 3 hojas reales, copiar `099_Setup.gs` y volver a correr `SIG_Setup_ConfiguracionColumnas()`, copiar `115_sigUD.gs`, `script_table.html`, `script_record.html`. Eliminar un registro de prueba y confirmar que desaparece de la tabla, del resumen, y (si tenía geometría vinculada) que el punto del mapa deja de mostrar su popup.

**SweetAlert2 para confirmaciones/alertas** (2026-09-02, a pedido del usuario — las confirmaciones eran `confirm()`/`alert()` nativos del navegador). `Index.html` suma SweetAlert2 por CDN — **ojo, la única librería del proyecto pinneada por versión mayor (`@11`) y no por versión exacta** (las demás son todas exactas: Bootstrap 5.3.7, Leaflet 1.9.4, DataTables 1.13.6, Chart.js 4.4.4) — no tenía a mano un número de parche exacto para confirmar que existe, así que se dejó así a propósito en vez de inventar uno; si se quiere fijar una versión exacta después, hay que confirmarla contra el CDN real. `script_utils.html` — nuevas `SIG.Utils.confirmar({titulo, texto, confirmarTexto, ...})` (devuelve `Promise<boolean>`) y `SIG.Utils.avisar(titulo, texto)`, envolviendo `Swal.fire`. `script_table.html` — el único `confirm()` nativo que había en todo el proyecto (el de Eliminar) ya usa `SIG.Utils.confirmar`.
**Para probar**: copiar `Index.html`, `script_utils.html`, `script_table.html`. Clic en Eliminar de un registro — el diálogo debería verse con el estilo de SweetAlert2, no el `confirm()` gris del navegador.
10. ⬜ Reportes/PDF dinámicos (el módulo más grande de `Version previa` — `pdf-designer.js` —, dejarlo para el final; mantenerlo en cliente, no en Apps Script, por el límite de 6 min de ejecución).

## Revisión Padre/Homúnculos (2026-09-08) — avance real vs `Version previa`

A pedido del usuario, Padre convocó a **Gula** (inventario de paridad módulo por módulo), **Soberbia** (integridad estructural — fuentes de verdad duplicadas) y **Pereza** (qué no conviene portar, y qué sobra ya) para comparar Motor SIG contra `Version previa/`, verificando contra código real en los dos lados, no contra documentación. Padre sintetizó (Modo 2) verificando él mismo varios puntos clave antes de arbitrar, y corrigió a dos de los tres Homúnculos con evidencia propia. Resumen de lo accionable — el detalle completo de cada hallazgo quedó en la conversación, esto es lo que hay que recordar para seguir:

**Avance confirmado, no a re-discutir**: CRUD completo (`Version previa` era 100% solo lectura), filtros en cascada, esquema/tabla/filtros sin un solo nombre de campo hardcodeado (`Version previa/js/filtros.js` sí los tenía tipeados a mano), geometrías reales vía `SIG_GEOMETRIAS`+GeoJSON con soporte de polígonos (`Version previa/js/mapa.js` solo detectaba puntos por heurística de nombre de columna), capas base leídas de las hojas en vez de archivos estáticos hardcodeados en el JS (`Version previa/js/geodata.js:97-98`), `google.script.run` envuelto en Promise (`SIG.Utils.run`).

**El hallazgo que condiciona todo lo demás**: `pdf-designer.js` real son 1718 líneas — más que todo el resto del frontend de Motor SIG junto — pero la única plantilla real que llegó a usarse (`Version previa/plantillas/index.json`, un solo archivo) usa apenas 3 de los 6 tipos de widget del motor. Gula/Soberbia/Pereza no coinciden en si conviene reconciliar el esquema (fuente `099_Setup.gs` vs hoja `CONFIGURACION_COLUMNAS`, más los cachés) *antes* del paso 10 — pero Padre resolvió el choque: **si el paso 10 termina siendo "imprimir la vista Resumen ya renderizada" (la versión mínima que propuso Pereza) en vez de un diseñador de layout completo, no hace falta elegir entre los tres mecanismos de filtro ni usar `VISIBLE_REPORTE` para nada — el argumento de urgencia de reconciliar ahora se cae solo.** La decisión de alcance del paso 10 va primero.

**Bugs/deuda nuevos que salieron de esta revisión, no estaban documentados**:
- `SIG_UD_get(hoja)` devuelve `{schema, rows}` que no es atómico — el esquema (`ud_schema_*`) y las filas (`ud_rows_*`) se invalidan por eventos completamente separados (`099_Setup.gs` solo invalida el primero, `SIG_UD_saveRow` solo el segundo). El bug de `"Requested unknown parameter 'COMUNIDAD'"` (documentado arriba, paso 8) no fue un incidente aislado — es la firma de esta clase de bug, verificado por Padre en el código, va a repetirse cada vez que se ajuste el esquema de una unidad sin correr `SIG_Cache_resetTodasLasUnidades()` después.
- Escribir una columna que está en `CONFIGURACION_COLUMNAS` pero no en el encabezado real de la hoja se acepta, se "guarda" y se pierde **con éxito reportado al usuario** — nadie compara el esquema declarado contra la hoja real en ningún punto del flujo.
- `LONGITUD`/`LATITUD` en `UD_RIEGOS` (`099_Setup.gs:76-77`) no tienen **ningún** lector en todo el proyecto (confirmado por grep), pero `script_record.html` no las excluye del formulario de alta/edición — se muestran como campos editables, se guardan, y nunca se usan. Mismo patrón que el ejemplo de la sección 7 del perfil del usuario (`assigned_route_ref`/`driver_profile.assigned_route_id`).
- De los valores que puede tomar `UI` en el esquema (`SEL`/`HID`/`texto`), **solo `SEL` tiene lector real** (`script_summary.html:76`) — `HID` no lo respeta nadie, por eso columnas marcadas como ocultas (`DEPARTAMENTOS`, `CATEGORIA PROGRAMATICA`, `LONGITUD`, `LATITUD`, etc.) igual aparecen en el formulario de registro. La línea de este mismo README que dice que esas columnas están "ocultas por uso interno" (paso 2) ya no es del todo cierta en la práctica.
- `DOCUMENTOS_ID` es una FK declarada en las 3 unidades sin ningún lector ni escritor real en todo el código — el módulo de documentos adjuntos de `Version previa` (`esquema.js`) nunca se portó ni quedó anotado como pendiente.
- `SIG.Utils` no tiene ningún helper de formato (moneda, área, entero/decimal) — `Version previa/js/utils.js` sí (`formatMoney`, `formatAreaValue`). El esquema ya sabe qué columnas son `NUM`/`DEC`, pero `script_table.html` no tiene ninguna función que lo traduzca a formato visual. Es el único punto ciego que los tres Homúnculos avalan a la vez, sin tocar arquitectura.
- El ruteo por URL/hash que sí tenía `Version previa/js/estado.js` (URLs compartibles, botón atrás del navegador funcional, persistencia de filtros al cambiar de unidad) no está en Motor SIG — y no está mencionado en este README ni como hecho ni como pendiente. Punto ciego real, no una regresión silenciada a propósito.

**Recomendado, en orden**:
1. **Antes de agregar cualquier funcionalidad nueva**: confirmar en el Apps Script real que los pasos 6 (gráficas) y 9 (capas por unidad) — hoy 🔶, escritos pero no verificados en vivo — funcionan de punta a punta con las 3 unidades reales.
2. Agregar los helpers de formato a `SIG.Utils` (moneda/área), consumidos por `script_table.html` según `campo.tipo` — chico, aislado, reversible, sin decisión de arquitectura de por medio.
3. Un "detector, no refactor": una función que compare `CONFIGURACION_COLUMNAS` contra el encabezado real de cada hoja `UD_*` y **solo reporte** las diferencias (no decide nada, no corrige nada solo). Hace visible el descarte silencioso que hoy pierde datos con éxito reportado.

**Preguntas que Padre devolvió explícitamente al usuario — sin responder todavía, no asumir nada**:
1. **Alcance del paso 10** (bloqueante, condiciona el resto): ¿"imprimir la vista Resumen con sus filtros aplicados" (versión mínima), o un diseñador de layout con plantillas guardadas como tenía `Version previa`?
2. ~~`LONGITUD`/`LATITUD` de `UD_RIEGOS`: ¿tienen datos reales cargados en la hoja viva?~~ **Respondida y verificada (2026-09-11)**: sí tenían datos (20 de 114 filas), pero las 20 **también** tenían `SIG_GEOMETRIAS_ID` cargado — cero filas dependían solo de lon/lat. Confirmado con `openpyxl` contra la hoja real antes de tocar nada (no se asumió por lo que dijo el usuario). Se declararon muertas: sacadas de `CONFIGURACION_COLUMNAS`, columnas físicas de la hoja intactas. Ver sección de importación de Riego más abajo.
3. ¿Colapsar el array literal de `099_Setup.gs` con la hoja `CONFIGURACION_COLUMNAS` en una sola fuente editable sin tocar código? (Se pierde tener una semilla reproducible desde cero.)
4. El chequeo de coherencia esquema↔hoja real (punto 3 de "Recomendado" arriba): ¿ahora, o después de resolver el alcance del paso 10?
5. Ruteo por hash/URL: ¿se suma al roadmap como paso nuevo, o se descarta explícitamente? Hoy no es ni una cosa ni la otra.

## Importación de datos Riego: TANQUES/UGR/GEOMEMBRANA (2026-09-11)

A pedido del usuario ("en la carpeta de googlesheet he agregado el actual excel... ademas un excel y un json de datos que todavia no tenemos. Eso debe agregarse a UD_Riegos..."). Fuentes: `googlesheet drive/BASE DE DATOS PROGRAMA GEOMEMBRANA.xlsx` (4 hojas: `TANQUES`, `UGR`, `GEOMEMBRANA`, `RESULTADO` — esta última es un pivot agregado, se ignoró) y `googlesheet drive/BASE DE DATOS 2026 PROGRAMA SEGURIDAD ALIMENTARIA_2026-07-30_15-07-38.json` (export VBA, **UTF-16 con BOM** — falla con `UnicodeDecodeError` si se lee como UTF-8) usado solo para validar, no como fuente primaria (no trae CEMENTO/FIERRO/GEOMEMBRANA, que solo están en el Excel).

**Validación de zona UTM** (mismo método que Forestación, zona distinta): el Excel trae coordenadas en ESTE/NORTE sin especificar zona. Se convirtió el registro Cocapata/Geomembrana `(747736.88, 8140722.01)` con `pyproj` contra **EPSG:32719 (zona 19S)** → `lon=-66.6755349, lat=-16.8035652`, que calza exacto (7 decimales) con el mismo registro en el JSON. **No es zona 20S** como Forestación — se probó también y da un `lon` claramente incorrecto (~-60.68). Confirmado antes de convertir ninguna fila más.

**Celda con múltiples puntos** (el usuario avisó que existía, se buscó explícitamente con un script que escanea `\n` en todas las celdas de las 3 hojas): una sola fila, `TANQUES` fila 18, Capinota/"VILLCABAMBA-COCOMA", con `ESTE="797668\n801325"`. El JSON de referencia (IDs 16/17) confirmó que esto son **2 proyectos independientes** (comunidades "VILLCABAMBA" y "COCOMA" por separado, cada una con familias/área/plazo **completos**, no repartidos) — se verificó cruzando las 2 coordenadas convertidas contra los 2 `lat`/`lon` del JSON, match exacto. Se separó en 2 filas de `UD_RIEGOS` con geometría propia cada una (tipo `POINT`, sin agregar `MULTIPOINT` al catálogo `SIG_TIPO` — hubiera sido una categoría de tipo nueva para un solo caso, y el propio JSON ya confirmaba que en realidad son 2 registros, no 1 con 2 puntos). `CEMENTO`/`FIERRO` de esa fila (178/364 combinados) sí se repartieron 89/182 cada una — no es una división arbitraria, es exactamente el valor modal de un tanque estándar en toda la hoja, la suma cuadra.

**Filas excluidas** (10 de las 33+25 filas de dato de `TANQUES`/`UGR`, además de las filas `TOTAL` de cierre de cada hoja): 5 en `TANQUES` y 5 en `UGR`, mismos municipios en ambas hojas (Capinota/Sicaya, Tapacarí/Tacopaya, Punata/Tacachi, Arani/Arani, German Jordán/Toco) — filas con provincia/municipio pero **sin ningún otro dato** (ni comunidad, ni beneficiarios, ni coordenada utilizable). En `UGR` estas mismas filas tenían un valor de `ESTE` que resultó ser un residuo de celda combinada del Excel (idéntico, dígito por dígito, al de otra fila real más abajo con municipio distinto) — se verificó explícitamente antes de descartarlas, no se asumió. El total post-limpieza (29 `TANQUES` + 20 `UGR` + 11 `GEOMEMBRANA` = 60) coincide exacto con el conteo de `Registros` del JSON en sus 3 hojas — confirmación cruzada de que el criterio de exclusión usado es el mismo que ya se aplicó al generar el JSON oficial.

**Región/Provincia**: la columna `REGION` del Excel viene de una celda combinada en varias filas (se repite el valor de arriba) y tiene errores de tipeo reales (p. ej. `UGR` fila 8 trae `REGIONES="AYOPAYA"`, que es un nombre de provincia, no de región; `ESTEBAN ARCE`/`VALLES ALTO` en vez de `Valles`). En vez de usar esa columna se derivó `REGIONES_ID` cruzando `PROVINCIAS_ID` contra el catálogo real (`PROVINCIAS`/`REGIONES`), con 2 alias de nombre (`ESTEBAN ARCE`→`Esteban Arze`, `JOSE CARRASCO`→`Carrasco`, mismo problema de variantes de nombre que ya documentaba Forestación). **Ojo**: para Capinota el catálogo dice `Andina` pero el JSON de referencia trae `REGIONES="VALLES"` para ese mismo registro — se priorizó el catálogo interno (es el que usan los filtros/capas del propio SIG) pero la discrepancia no se investigó a fondo, queda anotada acá por si el usuario la reconoce como un error conocido de alguna de las 2 fuentes.

**3 municipios sin catálogo** (mismo patrón ya documentado en la importación de Forestación — `MUNICIPIOS` real le faltan filas): `INDEPENDENCIA` (Ayopaya — ya era el mismo caso sin resolver de Forestación), `RAQAYPAMPA` (Mizque), `"VILLA G. VILLARROEL"` (Punata, probablemente la misma "Villa Guarlberto Villarroel" sin resolver de Forestación con otra abreviatura). Como `UD_RIEGOS.MINICIPIOS_ID` guarda texto plano (no un ID real — inconsistencia de nombre ya documentada antes en este README) esto no bloqueó el import, solo se anota para cuando el usuario complete el catálogo.

**Cambios en `099_Setup.gs`** (`SIG_Setup_ConfiguracionColumnas`, bloque `UD_RIEGOS`):
- Sacadas `LONGITUD`/`LATITUD` (pregunta 2 de la revisión Padre, respondida arriba).
- Agregadas `CEMENTO` (NUM, bolsas), `FIERRO` (NUM, barras), `GEOMEMBRANA_M2` (DEC, m²) — `SUM`/gráfico `BAR`, visibles en tabla y reporte. Vacías para los 114 proyectos previos, solo aplican a los 3 programas nuevos.
- Nuevas categorías en `CATEGORIAS` (mecanismo `CATEGORIA_ID` que ya existía, no uno nuevo — "cada hoja sería una categoría" del pedido del usuario): `8=TANQUES`, `9=UGR`, `10=GEOMEMBRANA`.

**Corrección sobre la marcha — `CATEGORIAS` no tenía 3 IDs libres, los tenía reservados**: al ir a aplicar los CSV, `CATEGORIAS` ya tenía filas con `ID=8,9,10` en la hoja real, solo con el nombre en blanco (se había leído mal en la primera pasada — un `(8, None, None)` se interpretó como fila vacía cuando en realidad el `8` era el `ID` ya ocupado). Agregar esos 3 IDs como filas nuevas habría duplicado el `ID`. Se corrigió: en vez de agregar filas, se completa el nombre de esas 3 filas ya existentes. `import_CATEGORIAS_riego.csv` se reescribió con esta instrucción (ya no es un CSV para pegar).

**Aplicado ya a la copia local** (`googlesheet drive/SDMAyRH Centralizador.xlsx`, a pedido del usuario, 2026-09-11 — no solo generados los CSV, sino escritos directo con `openpyxl` sobre el archivo real): se guardó un backup (`SDMAyRH Centralizador - backup antes de import riego 2026-09-11.xlsx`) antes de escribir. Verificado releyendo el archivo guardado desde cero (no el objeto en memoria): `CATEGORIAS` con los 3 nombres completados, `UD_RIEGOS` 115→175 filas (174 con datos, `ID` hasta 174), `SIG_GEOMETRIAS` 269→328 filas (327 con datos), `CONFIGURACION_COLUMNAS` con 33 filas de `UD_RIEGOS` (30 originales −2 por `LONGITUD`/`LATITUD` +3 por `CEMENTO`/`FIERRO`/`GEOMEMBRANA_M2`). La fila de Capinota quedó separada en dos (`VILLCABAMBA` `ID=126`↔geometría `279`, `COCOMA` `ID=127`↔geometría `280`).

**Pendiente, esto es lo que falta para que llegue a producción**: la copia local es un espejo, no la hoja viva — el Google Sheet real todavía no tiene estos cambios. Falta: (1) subir/sincronizar esta copia local a Google Sheets (o pegar los mismos 3 archivos CSV a mano ahí, ajustando `CATEGORIAS` según la corrección de arriba), (2) copiar `099_Setup.gs` al proyecto de Apps Script y correr `SIG_Setup_ConfiguracionColumnas()` (sincroniza `CONFIGURACION_COLUMNAS` real con el array del código), (3) correr `SIG_Cache_resetTodasLasUnidades()`. Recién ahí la tabla de Riego va a mostrar 174 filas con `CEMENTO`/`FIERRO`/`GEOMEMBRANA_M2`, el mapa ~79 puntos de riego (59 nuevos + 20 previos), y el formulario de edición ya no debería mostrar Longitud/Latitud.

**Archivos CSV** (siguen sirviendo para la hoja viva de Google Sheets si no se sincroniza el `.xlsx` completo), todos en `googlesheet drive/`:
- `import_CATEGORIAS_riego.csv` — ya **no** es un CSV para pegar, quedó como instrucción de qué completar a mano (ver corrección arriba).
- `import_SIG_GEOMETRIAS_riego.csv` — 59 filas (`ID 269-327`), pegar al final de `SIG_GEOMETRIAS`. Un punto por proyecto georreferenciado, `CAPA_ID=1`/`ESTILO_ID=1`/`TIPO_ID=1` (mismo trío que usan las 20 geometrías de riego ya existentes, sin excepción — confirmado antes de asumirlo).
- `import_UD_RIEGOS_nuevos.csv` — 60 filas (`ID 115-174`), pegar al final de `UD_RIEGOS`. 1 fila sin geometría (Esteban Arze/Anzaldo/Cabrera — tiene comunidad y CEMENTO/FIERRO pero ninguna coordenada en el Excel origen, se dejó `SIG_GEOMETRIAS_ID` vacío en vez de inventar una ubicación).

## Notas operativas

- Python real de este equipo (no usar `python`/`python3` a secas en Bash, cae en el stub de Windows Store): `C:\Users\PC04\AppData\Local\Programs\Python\Python314\python.exe` (tiene `openpyxl` para leer el `.xlsx` local).
- El ID real del spreadsheet vive en `002_Constantes.gs` (`SIG_DB.SPREADSHEET_ID`) — no se repite aquí a propósito, para no tener dos fuentes de verdad si cambia.
